{
  "name": "webnovel-metadata-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Collectors for KakaoPage and Naver Series webnovel metadata.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "kakao": "node --env-file=.env src/cli/kakao.js",
    "naver": "node --env-file=.env src/cli/naver.js",
    "test": "node --test"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
